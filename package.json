{
  "name": "agent-game-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Event-sourced game runtime with Pi Agent as the only agent integration layer.",
  "exports": {
    "./sdk": {
      "types": "./dist/sdk/index.d.ts",
      "import": "./dist/sdk/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:examples": "tsc -p examples/tsconfig.json",
    "test:examples": "npm run build && npm run build:examples && node --test dist-examples/**/*.test.js",
    "test:conformance": "npm run build && npm run build:examples && node --test dist/sdk/conformance.test.js dist-examples/conformance/*.test.js",
    "check:modules": "node scripts/check-module-boundaries.mjs",
    "verify:release": "npm run test && npm run check:modules && npm run test:conformance",
    "start": "npm run build && node dist/app/main.js",
    "test": "npm run build && npm run build:examples && node --test dist/**/*.test.js && node --test dist-examples/**/*.test.js",
    "import:atlas:fuyuki": "node scripts/download-atlas-fuyuki.mjs",
    "index:atlas:fuyuki": "npm run build && node scripts/index-atlas-fuyuki.mjs",
    "import:atlas:canon": "node scripts/download-atlas-canon.mjs",
    "index:atlas:canon": "npm run build && node scripts/index-atlas-canon.mjs",
    "import:atlas:servants": "node scripts/download-atlas-servants.mjs",
    "index:atlas:servants": "npm run build && node scripts/index-atlas-servants.mjs"
  },
  "dependencies": {
    "@deepseek-ai/cordis": "4.0.1",
    "@earendil-works/pi-ai": "0.84.1",
    "@earendil-works/pi-coding-agent": "0.84.1",
    "typebox": "1.3.12"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "typescript": "7.0.2"
  }
}
